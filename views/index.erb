<div class="row">
  <div class="col-md-6">
    <h3>Trains:</h3>
    <% @trains.each do |train| %>
      <%= train['name'] %>
    <% end %>
  </div>
  <div class="col-md-6">
    <h3>Cities:</h3>
    <% @cities.each do |city| %>
      <%= city['name'] %>
    <% end %>
  </div>
</div>
